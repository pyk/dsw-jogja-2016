# List
# Define a list of number
numbers = [1, 4, 12, 1]
# Define a list of string
words = ['one', 'two', 'three']
# Access 3rd element of list of numbers, index=2
element = numbers[2] # element=12
# Update the 3rd element
numbers[3] = 10
# Insert new element
numbers.append(67)
# Delete the first element, index=0
numbers.pop(0)

# List Operations
# Get the number of elements
len(['hello', 'there']) # 2
# Concatenation
concat = [1, 2, 3] + [4, 5, 6] # [1, 2, 3, 4, 5, 6]
# Membership
is_exists = 3 in [1, 2, 3] # True
is_exists = 4 in [1, 2, 3] # False
# Repetition
repeat = ['Re'] * 3 # ['Re', 'Re', 'Re']



